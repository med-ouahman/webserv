
#pragma once

namespace http {

namespace limits {

enum {
	REQUEST_LINE_MAX_SIZE = 1024,
	HEADER_MAX_SIZE = 1024 * 4,
	BODY_MAX_SIZE = 1024 * 1024,
	CHUNK_SIZE_LINE_MAX = 1024,
	BODY_BUFFER_SIZE = 1024 * 16,
	BODY_PROGRESS_MIN_BYTES = 4096
};

}

namespace timeout {

enum {
	REQUEST_LINE_SECONDS = 5,
	HEADER_SECONDS = 10,
	BODY_PROGRESS_SECONDS = 30,
	BODY_MIN_WAIT_SECONDS = 5,
	BODY_MIN_BYTES_PER_SECOND = 200
};

}

}
