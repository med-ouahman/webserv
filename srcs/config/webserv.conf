server {
    listen 8080;
    server_name localhost;
    root ./www;
    index index.html index.htm;

    location / {
        root ./www;
        allowed_methods GET POST;
    }

    location /images {
        root ./images;
        allowed_methods GET;
    }
}